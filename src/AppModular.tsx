import React from 'react';

// Stub mínimo para mantener el archivo compilable y evitar errores mientras App.tsx es la entrada principal.
const AppModular: React.FC = () => {
  return (
    <div className="min-h-screen bg-slate-900 text-slate-100 p-6">
      <div className="max-w-2xl mx-auto">
        <h1 className="text-xl font-semibold">APU Presupuestos</h1>
        <p className="mt-2 text-slate-300">
          Vista modular temporalmente desactivada. La app utiliza actualmente la vista base (App.tsx).
        </p>
      </div>
    </div>
  );
};

export default AppModular;


